import { describe, expect, it } from "vitest";

describe("@monolab/ts-types", () => {
    it("should export the package successfully", () => {
        // This test verifies that the package can be imported without errors
        expect(true).toBe(true);
    });
});
