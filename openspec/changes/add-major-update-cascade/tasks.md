# Tasks — add-major-update-cascade

> Implementation = authoring/editing the skill + command markdown under `claude-plugins/experiments/`. No automated tests in this plugin; verification is manual (Phase 4). Do NOT hand-edit plugin/package versions — release-please drives the bump from conventional commits. Do NOT edit `openspec/specs/` while this change is in-flight (the deltas under this change are the contract). Builds on `add-minor-update-cascade` (MON-200) — see Task 0.

## 0. Prerequisite — minor platform must be present (hard gate)

- [x] 0.1 **DONE** — the `feature/MON-202-commander-update-deep-major` worktree was rebased onto `develop` (bcc8376). Confirmed present: `claude-plugins/experiments/skills/apply-npm-updates/SKILL.md`, the orchestrator delegating per-project apply to it, the four minor commands, and `parallel-research-workflow` emitting `## <Level> bump set` + `## Changelogs`. The synced base specs (`openspec/specs/npm-update-apply`, `…/commander-update-orchestrator-skill`, `…/parallel-research-workflow`) are present, so this change's MODIFIED deltas resolve.
- [ ] 0.2 Capture a non-regression baseline: on a clean tree run `/experiments:npm-update-patch` and `/experiments:npm-update-minor` against a fixture workspace and save the transcripts (table, prompts, ncu/install, summary). These are re-checked in Phase 4.

## 1. Platform deltas (the three "non-homónima" gaps)

- [x] 1.1 `skills/apply-npm-updates/SKILL.md` — replace "`target` passed verbatim to `ncu --target`" with a `level → ncuTarget` resolution (`patch→patch`, `minor→minor`, `major→latest`, `engines→latest`+`--enginesNode`), mirroring `scan-npm-updates`. Pass `ncuTarget` to every `ncu --target` call. Keep validation `target ∈ {patch,minor,major,engines}`. Mapping is identity for patch/minor (their target is unchanged; their written version changes via 1.2c).
- [x] 1.2 `skills/apply-npm-updates/SKILL.md` — when `ncuTarget === latest` (major/engines), ALWAYS include `--filter "<names>"` (the `names` list is authoritative; treat `includeFilter` as `true` regardless). Document why (scan's major set ⊂ ncu `latest` candidate set). Update the "Level-agnostic operation" + hard-rules wording to reflect the mapping.
- [x] 1.2c `skills/apply-npm-updates/SKILL.md` — **exact pinning, family-wide:** always pass `--removeRange` on every `ncu` bump so deps are written exact (`19.0.2`, no `^`/`~`), at all levels and bump types; and make `catalogEdits` strip the leading range operator so catalog entries pin exact too. Update the "Level-agnostic operation" requirement to note `--removeRange` is uniform. (Decided: "en general cualquier update pinea", todos los niveles. Supersedes the dropped `manifestPins`/cap idea.) Note: this changes shipped patch/minor output — see 4.5.
- [x] 1.3 `skills/commander-update-orchestrator/SKILL.md` — in the per-project apply-spec builder (Step 10), set `includeFilter: true` for every `manifestBumps` element when `target` maps to `latest` (major/engines); note that target→ncu mapping is delegated to `npm-update-apply`. patch/minor branch unchanged.
- [x] 1.4 `skills/parallel-research-workflow/SKILL.md` — phase-2 research contract: for `level=major`, add the mandatory `### Breaking changes & migration` finding category (required code/config changes, removed/renamed/changed APIs, codemods, deprecations; `_no findings_` sentinel) to BOTH the single-project and cross-project subagent prompt templates (cross-project: universal phrasing, no specific project paths). Level-gated — no change for patch/minor/engines.
- [x] 1.5 `skills/parallel-research-workflow/SKILL.md` — phase-4 `plan.md` synthesis: for `level=major`, emit a `## Breaking changes & migration` H2 before `## Improvements` (both modes), with `_no breaking changes_` sentinel when empty. Update the plan.md section enumeration. Confirm the `## <Level> bump set` heading renders `## Major bump set`.
- [x] 1.6 **(DROPPED)** The per-project `manifestPins`/cap idea is removed: exact pinning is now uniform via `--removeRange` (1.2c), and a major run bumps a project to latest or doesn't touch it — there is no sub-latest cap to special-case. No orchestrator pin logic needed beyond the `includeFilter` force (1.3).

## 1b. Isolation & breaking-change PR grouping (new skills)

- [x] 1b.1 Create `skills/update-isolation/SKILL.md`: inputs `{ projectPath, branchName, strategy }`; resolution `auto` (worktrunk via `wt` if `command -v wt` and repo usable → else `git worktree add -b`), plus `ask` / `branch` / `worktree` / `none`. Detect worktrunk; honor the apply install-skip when a worktrunk hook already installed. Returns `{ mode, branchName, workdir }`. Hard rules: branch/worktree only — SHALL NOT commit, push, or open a PR; worktree modes leave the current checkout untouched.
- [x] 1b.2 Create `skills/partition-breaking-changes/SKILL.md` (pure, no VCS/network): build HARD co-upgrade sets (peer/lockstep + override-registry families + a `peerDependencies` read), score risk per set (blast radius, breaking-change weight, centrality, codemod count), apply policy knobs (`isolateHighRisk`, `batchLowRisk`, `maxPackagesPerBucket`, `maxRiskPerBucket`), emit ordered buckets `{ title, packages, riskTier, rationale, suggestedBranch, suggestedMergeOrder }` + a count-by-policy table. Consume the `## Breaking changes & migration` findings + bump set + dep-graph read.

## 2. Major command wrappers (thin, on the now-major-ready flow)

- [x] 2.1 Create `commands/npm-update-major.md` (MON-138): scan `level=major` → table → prompt (with breaking-changes caution) → override resolution via `npm-update-apply` procedure → optional isolation gate (`update-isolation`, one target for the whole set) → `npm-update-apply` (`target: major`) in the resolved workdir → `## npm-update-major summary`; empty copy `No major updates available.`; major-namespaced abort copy. Mirror `npm-update-minor.md`.
- [x] 2.2 Create `commands/npm-update-deep-major.md` (MON-147): scan(major) → group → `parallel-research-workflow` (`level: major`) → `partition-breaking-changes` → surface `## PR plan` + opt-in isolation gate → exec prompt → per-bucket: `update-isolation` (worktree) then bumps via `npm-update-apply` (`target: major`, generic-only) + plan-mode round (improvements **and** breaking-change/migration items) into that workdir → `## npm-update-deep-major summary` (each bucket → branch/worktree path + next steps) → workflow cleanup. Hard rule: no override registry on the deep path; no commit/push/PR. Plan carries `## Major bump set` + `## Breaking changes & migration` + `## PR plan` + `## Changelogs`. Mirror `npm-update-deep-minor.md`.
- [x] 2.3 Create `commands/commander-update-major.md` (MON-196): thin wrapper invoking `commander-update-orchestrator` once with `level:"major"`, `target:"major"` (mode shallow/absent); stray-arg ignore line; surface verbatim. Mirror `commander-update-minor.md`.
- [x] 2.4 Create `commands/commander-update-deep-major.md` (MON-202): thin wrapper invoking the orchestrator once with `level:"major"`, `target:"major"`, `mode:"deep"`; stray-arg ignore line; surface verbatim (incl. `plan.md` with `## Breaking changes & migration` + `## PR plan` + `## Changelogs`). v1 isolation: one worktree per project (per-bucket cross-project deferred). Mirror `commander-update-deep-minor.md`.

## 3. Registration & docs

- [x] 3.1 Add the four new commands to the experiments plugin `README.md` command listing. Do NOT hand-edit the plugin version.
- [x] 3.2 Confirm plugin/marketplace manifests auto-discover the new command files (no manual manifest edit needed beyond README); confirm no stray references to deferred engines commands.
- [x] 3.3 **(DECIDED: touch them)** Family-wide hard-rule refinement: update the "Never create commits, branches, or PRs" line to "Never create commits or PRs (or push); branch/worktree isolation is allowed via `update-isolation`" across the four major commands, the orchestrator, AND the already-shipped patch/minor command files (`npm-update-patch`, `npm-update-deep-patch`, `npm-update-minor`, `npm-update-deep-minor`, `commander-update-{patch,minor}`, `commander-update-deep-{patch,minor}`) — add the same opt-in isolation gate (default `none`, so byte-equivalent unless chosen). `apply-npm-updates`' own hard rules are unchanged (it never did VCS). Not peeled into a separate change — folded here.

## 4. Manual verification matrix

- [ ] 4.1 `/experiments:npm-update-major`: empty result (`No major updates available.`); apply-all; pick-subset (exclude one); cancel; an override match (run-override / skip-matched / force-generic). Confirm `--target latest` + `--filter` is always used (no over-bump of minor/patch-only deps) and major caution + summary copy. **(QA: mechanical core verified via `/tmp` npm fixture — `ncu -p npm --target latest --upgrade --removeRange --packageFile pkg --filter "react react-dom"` bumped ONLY react+react-dom to exact `19.2.7`, left lodash/chalk/nanoid untouched; the same run WITHOUT `--filter` over-bumped chalk→5/nanoid→5/lodash→4.18, proving the forced filter. Interactive prompt flows — empty/apply-all/pick-subset/cancel/override — pending a live plugin session.)**
- [ ] 4.2 `/experiments:npm-update-deep-major`: apply-all (bumps + plan-mode improvements + breaking-change migration edits); apply-bumps-only; pick-subset; cancel; plan-mode rejection preserves bumps. Confirm `plan.md` has `## Major bump set` + `## Breaking changes & migration` + `## Changelogs`; no override registry consulted.
- [ ] 4.3 `/experiments:commander-update-major`: empty registry; multi-select subset; conflict-policy prompt; cancel; stop-on-fail partition. Confirm per-project apply always filters; registry `shasum` unchanged.
- [ ] 4.4 `/experiments:commander-update-deep-major`: full deep run across ≥2 projects — picker, deduped research, four-option gate, cross-project plan-mode round (improvements + breaking changes), summary. Confirm deduped `## Breaking changes & migration` + `## Changelogs`. **Exact-pin check:** bumped deps are written as exact versions (no `^`/`~`) in each project's `package.json` and aligned (same exact version across projects that bumped); catalog entries pinned exact. Registry `shasum` unchanged.
- [ ] 4.5 Shipped-command re-check: one `/experiments:npm-update-patch` and one `/experiments:npm-update-minor` run — confirm the only **intended** differences vs the Phase 0 baseline are (a) the **exact-pin** (deps now written without `^`/`~`) and (b) the **new opt-in isolation gate prompt** (default `none` → no VCS action; per the DECISION to add the same gate flow family-wide, Option B). Tables, summaries (now with an `Isolation:` line), ordering, and the actual file outcomes when `none` is chosen are otherwise equivalent (only the exact-pin changes bytes). NOTE: this supersedes the earlier "prompts unchanged" wording — the isolation prompt is a deliberate, uniform addition, not a regression. **(QA: exact-pin verified via `/tmp` fixture — `ncu --target minor --removeRange` wrote react→`18.3.1`, nanoid→`3.3.12` exact, untouched chalk kept `^4.1.2`. Full baseline-diff (0.2) + live prompt-flow run still pending.)**
- [x] 4.6 `update-isolation`: with worktrunk present (worktree+branch created, current checkout untouched); worktrunk absent (plain `git worktree` fallback); `ask` path (direct-branch vs worktree); `none` default (no VCS action). Confirm NO commit/push/PR in any path, and that worktrunk-hook installs are not duplicated by the apply step. **(QA done via `/tmp` git fixture: `git worktree add -b` created worktree+branch, original checkout stayed on `main`/clean/1-commit, no remotes ⇒ no push/PR, worktree HEAD == base ⇒ no commit; `wt -C <repo> list` usability probe exits 0 ⇒ `auto` takes worktrunk path. `ask`/`branch`/`none` are trivial variants; skipInstall dedup is the `installAlreadyRan` signal.)**
- [x] 4.7 `partition-breaking-changes`: a HIGH-risk set (React major + peer set) lands as a solo bucket; low-risk leaf majors batch into one bucket; co-upgrade peers never split across buckets; the count-by-policy table renders. Single-project per-bucket worktrees materialize with only that bucket's diff; cross-project deep-major produces one worktree per project (per-bucket deferred). **(QA: skill is pure — traced a concrete set {react,react-dom,@types/react,storybook,@storybook/react,chalk,nanoid}: peer set + override family computed as hard co-upgrade sets BEFORE risk and never split; React set → solo HIGH bucket; chalk+nanoid → batched LOW bucket; count-by-policy isolate-high+batch-low=3 vs one-per-set=4. Per-bucket-worktree materialization rides QA 4.6's verified worktree mechanism.)**
