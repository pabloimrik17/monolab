# Tasks — add-engines-update-cascade

> Implementation = authoring/editing skill + command markdown under `claude-plugins/experiments/`. No automated tests in this plugin; verification is manual (Phase 4). Do NOT hand-edit plugin/package versions — release-please drives the bump from conventional commits. Do NOT edit `openspec/specs/` while this change is in-flight (the deltas under this change are the contract). Builds on the minor (MON-200) AND major (MON-202) cascades — see Task 0 and design D9. `engines` ≠ ncu dependency level: it is a runtime/toolchain version bump (Node + pnpm/npm/yarn/bun + Deno + Bun-runtime), aligning + pinning every runtime surface.

## 0. Prerequisite — minor + major platform present (hard gate)

- [x] 0.1 **DONE** — this worktree was advanced onto `develop`+major (`23bc105`). Confirmed present: `skills/apply-npm-updates`, `skills/update-isolation`, the `level`-parameterized orchestrator with the `## Breaking changes & migration` section and family-wide `--removeRange` exact-pin, the four major commands, and `parallel-research-workflow` emitting the breaking-change + `## Changelogs` sections. Synced base specs are patch+minor; major is an active/unsynced change → engines MODIFIED deltas are authored additively (design D9), and `openspec validate --strict` passes.
- [ ] 0.2 Capture a non-regression baseline: on a clean tree run one existing dependency-level command (`/experiments:npm-update-patch` and `/experiments:commander-update-patch`) against a fixture and save transcripts (table, prompts, summary). Re-checked in 4.7 to prove the engines branch leaves dependency levels unchanged.

## 1. Engine platform (new machinery + reconciliation)

- [x] 1.1 Create `skills/detect-toolchain-surfaces/SKILL.md` (capability `engine-surface-scanning`, read-only): the surface-matcher table (design D2 — `package.json` engines/`packageManager`/`devEngines`/`volta`; `.nvmrc`/`.node-version`; `.tool-versions`/mise/asdf; Dockerfile `FROM`+`ARG *_VERSION`; GitHub Actions `setup-node`/`pnpm/action-setup`/`setup-deno`/`setup-bun`; GitLab CI image+`NODE_VERSION`; CircleCI image+orb params). Per locus record `{ file, engine, currentVersion, locus, kind }`. Engine coverage node/pnpm/npm/yarn/bun/deno incl. bun/deno dual role. Runtime/support/ambiguous classification (D3: `private`/non-publishable/root + non-`package.json` files = runtime; publishable + range = support; else ambiguous → flag). Intra-repo misalignment detection (runtime loci only). Report `unknownSurfaces` rather than guess. Return the structured inventory; no writes/installs.
- [x] 1.2 Create `skills/apply-engine-bumps/SKILL.md` (capability `engine-update-apply`, VCS-free): target resolution (D4 — Node→max `lts!==false` from `https://nodejs.org/dist/index.json`; pnpm/npm/yarn/bun→registry `latest`; Deno→latest release) with a confirm gate before any write; offline → skip engine with note OR accept user target, never fabricate. Exact-pin + align all runtime loci to the same version per engine (D5); surgical version-only edits; preserve `packageManager` `name@` prefix, drop+report the `+sha…` corepack hash. GitHub Actions SHA-pin safety (rewrite only the version input, never `uses:@<sha>` or its comment). Leave `support`/`unknownSurfaces`; ambiguous only after caller resolution. Return `{ resolvedTargets, applied[], skipped[], droppedHashes[], failure? }`; stream edits, no consumer summary copy.
- [x] 1.3 `skills/commander-update-orchestrator/SKILL.md` — add the `level=engines` branch: per-project scan → `detect-toolchain-surfaces`, per-project apply → `apply-engine-bumps`, cross-project alignment on the resolved engine version (one target/engine, reused), skip override-registry consultation, deep mode → `parallel-research-workflow(level=engines)` with no `## PR plan`/partition. Dependency levels (patch/minor/major) untouched.
- [x] 1.4 `skills/parallel-research-workflow/SKILL.md` — for `level=engines`: phase-1 fetch engine release notes (dedup once per engine/version), phase-2 breaking-change research over engine notes, phase-4 `plan.md` carries `## Breaking changes & migration` (from engine notes) and a `## Changelogs` section linking engine notes; slug + `_meta.json.level` record `engines`. Inert for patch/minor/major.
- [x] 1.5 `skills/npm-changelog/SKILL.md` — add engine release-note retrieval (implemented in `commands/npm-changelog.md` — the actual location; there is no `skills/npm-changelog` dir) (capability `npm-changelog-retrieval`): node (nodejs.org / `github.com/nodejs/node`), pnpm/yarn/npm (GitHub releases / CHANGELOG), deno/bun (GitHub releases), over `current → target`, keyed by engine+range, cached like package changelogs, `_no changelog available_` sentinel when missing. Package-changelog path unchanged.
- [x] 1.6 `skills/scan-npm-updates/SKILL.md` — remove `engines` from the level-input enum (now `patch|minor|major`); update the error message to `Expected patch|minor|major.`; `level=engines` aborts (the toolchain bump is handled by `detect-toolchain-surfaces`).
- [x] 1.7 **Reconciliation (composes with major, no observable behavior change — design D9):** `skills/apply-npm-updates/SKILL.md` — remove the dead `engines→latest+--enginesNode` row from the `level → ncuTarget` table (no command routes `engines` through ncu/apply). Keep `target ∈ {patch,minor,major}` consistent; do not alter patch/minor/major behavior.

## 2. Engines command wrappers (thin, on the engine platform)

- [x] 2.1 Create `commands/npm-update-engines.md` (MON-139): `detect-toolchain-surfaces` → resolve/confirm targets → ambiguity prompt (default leave) → render surfaces+targets → `apply-all`/`pick-subset`/`cancel` gate (runtime breaking-change caution) → optional isolation gate (`update-isolation`, one workspace) → `apply-engine-bumps` in the resolved workdir → `## npm-update-engines summary`; empty `No engine updates available.`; engines-namespaced abort copy. Mirror `npm-update-major.md` shape.
- [x] 2.2 Create `commands/npm-update-deep-engines.md` (MON-148): detect → `parallel-research-workflow(level=engines)` → plan with `## Breaking changes & migration` + `## Changelogs` (engine notes) → gate → optional isolation → `apply-engine-bumps` + plan-mode migration round (approved items only; rejection preserves bumps) → `## npm-update-deep-engines summary` → workflow cleanup. NO `## PR plan`/partition (single coordinated co-upgrade). Mirror `npm-update-deep-major.md`.
- [x] 2.3 Create `commands/commander-update-engines.md` (MON-197): thin wrapper invoking `commander-update-orchestrator` once with `level:"engines"`, `target:"engines"` (mode shallow/absent); stray-arg ignore line; surface verbatim. Mirror `commander-update-major.md`.
- [x] 2.4 Create `commands/commander-update-deep-engines.md` (MON-201): thin wrapper invoking the orchestrator once with `level:"engines"`, `target:"engines"`, `mode:"deep"`; stray-arg ignore line; surface verbatim (plan with `## Breaking changes & migration` + `## Changelogs`, no `## PR plan`). Mirror `commander-update-deep-major.md`.

## 3. Registration & docs

- [x] 3.1 Add the four engines commands to the experiments plugin `README.md` command listing (engines row, alongside patch/minor/major). Do NOT hand-edit the plugin version.
- [x] 3.2 Confirm the plugin auto-discovers the four new command files + two new skill dirs (verified: `plugin.json` has no `commands`/`skills` arrays — directories auto-discovered; version is release-please-driven, not hand-edited; no stray `--enginesNode` / "future engines" placeholders remain — the three deep-command non-goal notes + orchestrator description/When-to-use + README were updated to reflect engines shipped) (no manual manifest edit); confirm no stray/placeholder engines references remain elsewhere.

## 4. Manual verification matrix

- [x] 4.1 `detect-toolchain-surfaces` on a monolab-like fixture — **dry-run verified** (fixture at `/tmp/engines-fixture`: root engines/packageManager+sha/devEngines/volta, private app pin, publishable-lib `>=22` range, exact-pinned publishable lib, `.nvmrc` (misaligned 24.10.0), CI `setup-node`+`pnpm/action-setup` SHA-pinned, Dockerfile FROM+ARG, plus a `${{ matrix.node }}` expression). All loci detected w/ correct versions; runtime/support/ambiguous classified per D3; node misalignment flagged; expression surface → `unknownSurfaces` (not guessed); CI `@<sha> # vX` left as non-loci; fixture byte-identical (no writes) (root `engines`/`packageManager`/`devEngines`, app pins, a publishable-lib `engines.node` range, `.nvmrc`, CI `setup-node`+`pnpm/action-setup`, a Dockerfile): confirm all loci detected with correct `currentVersion`; apps/root/`.nvmrc`/CI/Docker classified `runtime`, publishable-lib range classified `support`, an exact-pinned publishable manifest flagged `ambiguous`; intra-repo misalignment flagged; an odd/unparseable surface listed under `unknownSurfaces` (not guessed); no writes.
- [ ] 4.2 `apply-engine-bumps`: Node resolves to latest LTS from the dist index; pnpm/deno/bun resolve to latest; confirm gate precedes any write; all runtime Node loci rewritten to the same exact version (no `^`/`~`); `packageManager` keeps `name@` prefix and the `+sha…` hash is dropped+reported; a GitHub Actions step has only its version input changed (the `@<sha>` and `# vX` comment untouched); `support`/`unknownSurfaces` untouched; offline → engine skipped with note, never fabricated; no commit/push/PR.
- [ ] 4.3 `/experiments:npm-update-engines`: empty (`No engine updates available.`); apply-all; pick-subset; cancel; ambiguity prompt resolves a locus; optional isolation creates a worktree (current checkout intact) with no commit/PR; caution + `## npm-update-engines summary` copy correct.
- [ ] 4.4 `/experiments:npm-update-deep-engines`: research targets engine release notes; `plan.md` has `## Breaking changes & migration` + `## Changelogs` (engine notes) and NO `## PR plan`; apply-all (bumps + approved migration edits); plan-mode rejection preserves bumps; cancel touches nothing.
- [ ] 4.5 `/experiments:commander-update-engines`: empty registry; multi-select subset; cross-project alignment to one engine version; cancel; stop-on-fail; registry `shasum` unchanged; publishable-lib support ranges untouched across projects.
- [ ] 4.6 `/experiments:commander-update-deep-engines`: deep run across ≥2 projects — picker, engine research deduped once per engine/version, gate, cross-project plan-mode migration round, summary; deduped `## Breaking changes & migration` + `## Changelogs`; runtime surfaces pinned exact and aligned to the same version across projects that bumped; registry `shasum` unchanged.
- [ ] 4.7 Non-regression: one `/experiments:npm-update-patch` (or `-minor`/`-major`) run vs the Phase 0 baseline — confirm the orchestrator `engines` branch (1.3), the scan `engines`-removal (1.6), and the `apply-npm-updates` dead-row removal (1.7) leave dependency-level behavior unchanged (the engines path is inert for those levels).
