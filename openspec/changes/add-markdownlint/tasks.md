## 1. Installation

- [x] 1.1 Add `markdownlint-cli@^0.44.0` to devDependencies in `package.json`
- [x] 1.2 Run `pnpm install` to install the dependency

## 2. Configuration

- [x] 2.1 Add markdownlint rule to `lint-staged.config.ts`: `"*.md": "markdownlint --fix"`
- [x] 2.2 Create `.markdownlintignore` file with exclusions:
  - openspec/
  - .claude/
  - .opencode/
  - node_modules/
  - dist/
  - coverage/
  - AGENTS.md
  - CLAUDE.md
  - **/CHANGELOG.md (auto-generated by Nx Release)
- [x] 2.3 Create `.markdownlintrc` file with custom rule configuration
- [x] 2.4 Update `.prettierignore` to exclude `**/CHANGELOG.md`
- [x] 2.5 Update `eslint.config.ts` to exclude `**/CHANGELOG.md` from ignores array

## 3. Testing

- [x] 3.1 Create a test markdown file with known linting issues (trailing spaces, inconsistent lists)
- [x] 3.2 Stage the test file and verify markdownlint runs via git hook
- [x] 3.3 Verify fixable issues are automatically corrected
- [x] 3.4 Test markdownlint detects unfixable errors
- [x] 3.5 Verify lint-staged integration works correctly
- [x] 3.6 Verify files in `.markdownlintignore` directories are not linted

## 4. Documentation

- [x] 4.1 Update `openspec/project.md` to document markdownlint in "Quality & Linting Tools" section (already present)
- [x] 4.2 Update `openspec/project.md` to mention markdownlint in "Git Hooks" section under pre-commit workflow (already present)

## 5. Validation

- [x] 5.1 Run lint-staged manually to verify markdownlint integration works
- [x] 5.2 Auto-fix all markdown files in repository
- [x] 5.3 Verify all markdown files in the repository pass markdownlint checks with custom configuration
